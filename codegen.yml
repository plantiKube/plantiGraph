overwrite: true
schema: "generatedSchema.graphql"
documents: "gql/initializationMutations/CreateClockMutation/CreateClockMutation.graphql"
#  - "gql/**/*.graphql"
generates:
  operations.ts:
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
